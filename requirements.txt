# ---- Core (Streamlit Cloud friendly) ----
streamlit>=1.39
requests>=2.31
python-dotenv>=1.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
rapidfuzz>=3.5
beautifulsoup4>=4.12
lxml>=4.9
openpyxl>=3.1
reportlab>=4.0
plotly>=5.18
pytrends>=4.9.2

# ---- Optional heavy NLP (uncomment locally / on VPS for richer extraction) ----
# The app auto-detects these and falls back gracefully when absent.
spacy>=3.7            # then run: python -m spacy download en_core_web_sm
keybert>=0.8
sentence-transformers>=2.5
